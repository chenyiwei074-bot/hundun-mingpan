      {/* ════════════ LAUNCHER MODAL ════════════ */}
      {showLauncher && (
        <div className="fixed inset-0 z-[100] flex items-center justify-center p-4 sm:p-8"
          style={{ background: '"'"'rgba(0,0,0,0.50)'"'"', backdropFilter: '"'"'blur(16px)'"'"', WebkitBackdropFilter: '"'"'blur(16px)'"'"' }}
          onClick={() => setShowLauncher(false)}>
          <div className="relative w-full max-w-[340px] rounded-[24px] px-6 py-8 sm:px-8 sm:py-10"
            style={{ background: white, boxShadow: '"'"'0 40px 100px rgba(0,0,0,0.20), 0 0 0 0.5px rgba(178,149,93,0.08)'"'"' }}
            onClick={e => e.stopPropagation()}>
            <button onClick={() => setShowLauncher(false)}
              className="absolute top-4 right-4 w-8 h-8 rounded-full flex items-center justify-center transition-all duration-300"
              style={{ color: mute }} aria-label="关闭">
              <svg width="14" height="14" viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.5"><line x1="3" y1="3" x2="13" y2="13"/><line x1="13" y1="3" x2="3" y2="13"/></svg>
            </button>
            <div className="text-center mb-7">
              <p className="text-[10px] tracking-[0.2em] font-semibold mb-3" style={{ color: gold }}>选择功能</p>
              <h2 className="font-serif font-bold tracking-[-0.03em] leading-[1.2] mb-2"
                style={{ fontSize: '"'"'clamp(1.3rem, 4vw, 1.6rem)'"'"', color: ink }}>你想探索什么</h2>
              <p className="text-[12px] leading-relaxed" style={{ color: mute }}>左右滑动，选择你想使用的玄学工具</p>
            </div>
            <div className="relative">
              <button onClick={() => { const el = document.getElementById('"'"'ls'"'"'); if(el) el.scrollBy({left: -300, behavior:'"'"'smooth'"'"'}) }}
                className="absolute -left-2 sm:-left-4 top-1/2 -translate-y-1/2 z-10 w-9 h-9 rounded-full flex items-center justify-center transition-all duration-300 hover:scale-110 active:scale-95"
                style={{ background: white, border: '"'"'1px solid rgba(178,149,93,0.12)'"'"', boxShadow: '"'"'0 2px 12px rgba(0,0,0,0.05)'"'"' }}>
                <svg width="13" height="13" viewBox="0 0 16 16" fill="none" stroke={gold} strokeWidth="2"><path d="M10 3l-5 5 5 5"/></svg>
              </button>
              <div id="ls" className="flex overflow-x-auto snap-x snap-mandatory scrollbar-hide"
                style={{ scrollbarWidth: '"'"'none'"'"', msOverflowStyle: '"'"'none'"'"' }}>
                {[
                  { href:"/bazi-chart", icon:"☯", title:"八字 & 紫微", desc:"四柱天干地支 + 十二宫排盘。大运流年、主星四化，一盘双参。", tag:"" },
                  { href:"/liuyao", icon:"🪙", title:"六爻问卦", desc:"金钱起卦，六爻纳甲。世应用神，一事一问，即时断卦。", tag:"" },
                  { href:"#", icon:"💞", title:"姓名合盘", desc:"二人姓名，五行适配。天格地格人格外格总格，五格剖象，深析缘分。", tag:"即将上线" },
                  { href:"#", icon:"📅", title:"择日", desc:"黄道吉日，天时地利。嫁娶开业搬家出行，择最优时日，趋吉避凶。", tag:"即将上线" },
                ].map((item, i) => (
                  <Link key={i} href={item.href}
                    onClick={() => item.tag ? null : setShowLauncher(false)}
                    className={item.tag ? '"'"'pointer-events-none group'"'"' : '"'"'group'"'"'}
                    style={{ textDecoration: '"'"'none'"'"', minWidth: '"'"'100%'"'"' }}>
                    <div className="snap-center w-full rounded-2xl p-6 flex flex-col items-center text-center transition-all duration-400 hover:shadow-md cursor-pointer"
                      style={{ background: paper, border: '"'"'0.5px solid '"'"' + hair }}>
                      <div className="w-16 h-16 rounded-full mb-4 flex items-center justify-center transition-transform duration-400 group-hover:scale-105"
                        style={{ background: '"'"'rgba(178,149,93,0.05)'"'"' }}>
                        <span className="text-2xl leading-none">{item.icon}</span>
                      </div>
                      <h3 className="font-serif font-bold text-lg tracking-[-0.02em] mb-1" style={{ color: ink }}>{item.title}</h3>
                      {item.tag ? (
                        <span className="text-[10px] font-medium tracking-[0.08em] px-2.5 py-0.5 rounded-full mb-3"
                          style={{ background: '"'"'rgba(178,149,93,0.08)'"'"', color: gold }}>{item.tag}</span>
                      ) : (
                        <div className="w-10 h-px mb-3" style={{ background: '"'"'rgba(178,149,93,0.20)'"'"' }} />
                      )}
                      <p className="text-[13px] leading-relaxed mb-5 max-w-[240px]" style={{ color: mute }}>{item.desc}</p>
                      <div className="flex items-center gap-1.5 transition-all duration-300 group-hover:gap-2.5"
                        style={{ color: item.tag ? '"'"'rgba(0,0,0,0.12)'"'"' : gold }}>
                        <span className="text-[13px] font-medium tracking-[0.03em]">{item.tag ? '"'"'敬请期待'"'"' : '"'"'进入'"'"'}</span>
                        <svg width="12" height="12" viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="2"><path d="M6 3l5 5-5 5"/></svg>
                      </div>
                    </div>
                  </Link>
                ))}
              </div>
              <button onClick={() => { const el = document.getElementById('"'"'ls'"'"'); if(el) el.scrollBy({left: 300, behavior:'"'"'smooth'"'"'}) }}
                className="absolute -right-2 sm:-right-4 top-1/2 -translate-y-1/2 z-10 w-9 h-9 rounded-full flex items-center justify-center transition-all duration-300 hover:scale-110 active:scale-95"
                style={{ background: white, border: '"'"'1px solid rgba(178,149,93,0.12)'"'"', boxShadow: '"'"'0 2px 12px rgba(0,0,0,0.05)'"'"' }}>
                <svg width="13" height="13" viewBox="0 0 16 16" fill="none" stroke={gold} strokeWidth="2"><path d="M6 3l5 5-5 5"/></svg>
              </button>
            </div>
            <div className="flex items-center justify-center gap-3 mt-6">
              {[0,1,2,3].map(i => (
                <button key={i} onClick={() => { const el = document.getElementById('"'"'ls'"'"'); if(el) el.scrollTo({left: i*310, behavior:'"'"'smooth'"'"'}) }}
                  className="w-1.5 h-1.5 rounded-full transition-all duration-300 hover:scale-125"
                  style={{ background: gold, opacity: 0.25 }} />
              ))}
            </div>
          </div>
        </div>
      )}